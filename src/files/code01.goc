// Return if a is greater than b
func gt(a: int, b: int) (bool) {
  if (a < b) {
    ret true;
  }

  ret false;
}

let x = 9;
let y: int = 10;

let c = gt(x, y);

while (!c) {
  x += 1
  c = gt(x, y)
}


print("${c}")